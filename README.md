# Image-classification-Supervised-vs-Self-Supervised-Learning
Image classification using CNNs (Supervised Learning) and compare the performance with CNNs trained in Self-Supervised Learning (even on the same dataset ignoring the labels), extracting the features and classifying them with a traditional classifier
